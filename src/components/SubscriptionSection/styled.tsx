import styled from "styled-components";

const Container = styled.section`
  background: url(/hero.png) no-repeat top right;
  min-height: 100vh;
`

const Content = styled.div`
  max-width: 128rem;
  margin-inline: auto;
`

export const StyledSubscriptionSection = {
  Container,
  Content
}